# 界面整改待办清单

> 目标：提高侧边栏场景的可理解性、可扫读性与操作效率；保持法律专业语感与结果可信度。

## 总体原则
1. 优先减少用户理解成本，再优化视觉一致性。
2. 改动不影响既有业务逻辑与接口，仅调整界面结构与呈现。
3. 每一步改造都保证可独立上线，不阻塞下一步。

## 第 1 阶段（低成本快速改造）
1. 给操作入口加文字标签，将纯图标工具栏改为“图标 + 文案”按钮或小卡片。
2. 在主界面顶部加入主流程提示：`1 提取文本 → 2 选择功能 → 3 查看结果/写回`。
3. 在未提取文本时，结果区显示清晰空态提示，并引导点击“提取选中段落”。
4. 给已提取文本增加状态卡：显示字数、提取时间、当前来源说明。
5. 统一所有结果卡头部布局（标题左、动作按钮右），动作按钮风格一致。
6. 所有“重新执行/写回 Word/导出”按钮统一命名与样式语义。
7. 对错误提示改为“就近提示”，优先显示在对应功能区域内。

## 第 2 阶段（结构优化与分组）
1. 将功能分组展示：
   - 文本审查
   - 索赔核定
   - 证据核查
   - 当事人提取
   - 身份证核查
   - 计算器
   - 文书工具
2. 计算器入口改为独立 Tab（或二级入口），避免跳出主流程视图。
3. 增加“最近运行时间”信息（每个结果卡顶部右侧标签）。
4. 在结果区顶部增加“概要结论条”（如：✅ 无异常 / ⚠️ 发现 3 项问题）。
5. 当功能运行时加入“处理中”占位卡，避免等待空白。

## 第 3 阶段（视觉一致性与专业感提升）
1. 建立统一色彩语义：成功=绿、警告=橙、错误=红、信息=蓝。
2. 统一卡片圆角、阴影、边框和标题字号，减少样式噪音。
3. 将说明文案改为法律专业语感，动作动词统一：审查/核定/提取/校验/写回。
4. 为“插入模板/传统诉状整理”增加说明提示或轻量引导。
5. 结果中涉及“写回标记”统一加标签说明（如“已写回 · 红色加粗”）。

## 第 4 阶段（可持续体验）
1. 允许历史结果折叠保留，避免新运行覆盖全部内容。
2. 为关键结果增加“复制摘要/一键导出”轻操作。
3. 为证据/对账单类结果增加筛选或折叠功能，提升长文档可读性。

## 验收清单（每阶段结束自查）
1. 用户不看说明也能明白第一步该做什么。
2. 用户在 10 秒内能完成一次功能调用。
3. 用户能快速区分“成功/警告/错误”三类输出。
4. 任何结果卡都能在 3 秒内扫读出核心结论。
5. 界面在 Word 侧边栏宽度下不挤压、不横向溢出。
